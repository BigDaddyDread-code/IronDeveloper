# PR135 - L4 Candidate Cannot Mutate Source/Memory Tests

PR135 adds regression tests proving Block M L4 candidate workflow outputs cannot mutate source or memory.

This PR adds no new workflow capability.

Candidate packages are review material only.

Candidate outputs cannot satisfy approval, satisfy policy, transition workflow, invoke tools, dispatch agents, call models, mutate source, apply patches, promote memory, mutate accepted memory, activate retrieval, create tickets, create incidents, write SQL, prove validation, prove release readiness, prove root cause, or prove repeated patterns.

Evidence is not approval. Proposal is not implementation. Preview is not execution. Package is not promotion. Approval package is not approval. Bundle is not proof. Pattern review is not diagnosis.

## Boundary

This is test armor only.

It does not add a new workflow, new candidate behavior, source apply, patch apply, memory promotion, accepted-memory mutation, retrieval activation, approval or policy satisfaction, workflow transition, tool invocation, agent dispatch, model call, prompt construction, SQL migration, API endpoint, CLI command, UI, hosted service, background worker, scheduler, orchestrator, runtime wiring, dogfood execution, test execution, fake approval ids, fake promotion ids, fake source-apply ids, or fake release-readiness ids.

SQL remains source of truth. Candidate output remains review material.

## Review line

PR135 checks the locks on every Block M package. It does not add a key.
