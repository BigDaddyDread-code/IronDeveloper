<UserControl x:Class="IronDev.Agent.Views.Workflow.ProjectHubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:IronDev.Agent.ViewModels.Workflow"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ProjectHubViewModel, IsDesignTimeCreatable=False}">

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="48,40,48,0">
            <TextBlock Text="PROJECT HUB" Style="{StaticResource IronEyebrow}"/>
            <TextBlock Text="Your Projects" Style="{StaticResource IronSectionTitle}"/>
        </StackPanel>

        <!-- Create New CTA -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="48,24,48,0">
            <Button Content="+ Create New Project"
                    Command="{Binding CreateNewProjectCommand}"
                    Style="{StaticResource IronPrimaryButton}"
                    Padding="32,14"/>
        </StackPanel>

        <!-- Project tiles -->
        <ScrollViewer Grid.Row="2" Margin="40,24,40,24">
            <ItemsControl ItemsSource="{Binding RecentProjects}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.OpenProjectCommand,
                                         RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"
                                Margin="10" Padding="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Style="{StaticResource IronTileCard}"
                                            Width="280" Height="160" Padding="22">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- Project name -->
                                            <TextBlock Grid.Row="0"
                                                       Text="{Binding Name}"
                                                       FontSize="17" FontWeight="Bold"
                                                       Foreground="{StaticResource TextBrush}"
                                                       TextTrimming="CharacterEllipsis"/>

                                            <!-- Description -->
                                            <TextBlock Grid.Row="1"
                                                       Text="{Binding Description}"
                                                       Style="{StaticResource IronMetaLabel}"
                                                       TextWrapping="Wrap"
                                                       Opacity="0.6"
                                                       Margin="0,6,0,0"/>

                                            <!-- Status + Model row -->
                                            <StackPanel Grid.Row="2" Orientation="Horizontal">
                                                <Border Style="{StaticResource IronStatusBadge}" Margin="0,0,8,0">
                                                    <TextBlock Text="{Binding IndexingStatus, TargetNullValue='Ready'}" 
                                                               Foreground="{Binding IndexingStatus, Converter={StaticResource StatusToBrushConverter}}"
                                                               FontSize="11"/>
                                                </Border>
                                                <Border Style="{StaticResource IronStatusBadge}">
                                                    <TextBlock Text="gpt-4o" FontSize="11"
                                                               Foreground="{StaticResource MutedBrush}"/>
                                                </Border>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
