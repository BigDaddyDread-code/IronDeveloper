<UserControl x:Class="IronDev.Agent.Views.Workflow.ProjectHubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:IronDev.Agent.ViewModels.Workflow"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ProjectHubViewModel, IsDesignTimeCreatable=False}">

    <UserControl.Resources>
        <!-- Dedicated Project Tile Style to avoid conflicts with global IronTileCard -->
        <Style x:Key="IronProjectTile" TargetType="Border">
            <Setter Property="Background"      Value="{StaticResource Background/Elevated}" />
            <Setter Property="BorderBrush"     Value="{StaticResource Border/Subtle}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius"    Value="14" />
            <Setter Property="Padding"         Value="22" />
            <Setter Property="MinWidth"        Value="320" />
            <Setter Property="MinHeight"       Value="180" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="4" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="48,40,48,0">
            <TextBlock Text="PROJECT HUB" Style="{StaticResource Typography.Eyebrow}"/>
            <TextBlock Text="Your Projects" Style="{StaticResource Typography.ScreenTitle}"/>
        </StackPanel>

        <!-- Create New CTA -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="48,24,48,40">
            <Button Content="+ Create New Project"
                    Command="{Binding CreateNewProjectCommand}"
                    Style="{StaticResource AppButton.Primary}"
                    Padding="32,14" Height="48"/>
        </StackPanel>

        <!-- Project tiles -->
        <ScrollViewer Grid.Row="2" Margin="40,0,40,40" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding RecentProjects}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.OpenProjectCommand,
                                         RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"
                                Margin="10" Padding="0" Cursor="Hand" Height="Auto"
                                OverridesDefaultStyle="True">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="TileBorder" Style="{StaticResource IronProjectTile}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/> <!-- Name -->
                                                <RowDefinition Height="Auto"/> <!-- Date -->
                                                <RowDefinition Height="*"/>    <!-- Summary -->
                                                <RowDefinition Height="Auto"/> <!-- Chips -->
                                            </Grid.RowDefinitions>

                                            <!-- Project name -->
                                            <TextBlock Grid.Row="0"
                                                       Text="{Binding Name}"
                                                       FontSize="18" FontWeight="Bold"
                                                       Foreground="{StaticResource Text/Primary}"
                                                       TextTrimming="CharacterEllipsis"/>

                                            <!-- Date -->
                                            <TextBlock Grid.Row="1" 
                                                       Text="{Binding CreatedDate, StringFormat='Created {0:MMM dd, yyyy}'}"
                                                       FontSize="11" Opacity="0.4" Margin="0,2,0,0"/>

                                            <!-- Description -->
                                            <TextBlock Grid.Row="2"
                                                       Text="{Binding Description}"
                                                       Style="{StaticResource Typography.BodySmall}"
                                                       TextWrapping="Wrap"
                                                       Opacity="0.7"
                                                       Margin="0,12,0,12"/>

                                            <!-- Status + Model row -->
                                            <StackPanel Grid.Row="3" Orientation="Horizontal">
                                                <TextBlock Text="{Binding IndexingStatus, TargetNullValue='Ready'}"
                                                           Foreground="{Binding IndexingStatus, Converter={StaticResource StatusToBrushConverter}}"
                                                           FontSize="10" FontWeight="Bold" Margin="0,0,12,0"
                                                           VerticalAlignment="Center"/>
                                                <TextBlock Text="GPT-4O" FontSize="10" FontWeight="Bold"
                                                           Foreground="{StaticResource Accent/Primary}"
                                                           VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="TileBorder" Property="Background" Value="{StaticResource Background/Interactive}"/>
                                            <Setter TargetName="TileBorder" Property="BorderBrush" Value="{StaticResource Accent/Primary}"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="TileBorder" Property="Opacity" Value="0.8"/>
                                            <Setter TargetName="TileBorder" Property="RenderTransform">
                                                <Setter.Value><ScaleTransform ScaleX="0.98" ScaleY="0.98"/></Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
