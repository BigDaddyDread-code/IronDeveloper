<UserControl x:Class="IronDev.Agent.Views.Workflow.ProjectHubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:IronDev.Agent.ViewModels.Workflow"
             xmlns:controls="http://schemas.irondeveloper.com/controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ProjectHubViewModel, IsDesignTimeCreatable=False}">

    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="420"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Grid.Column="0" Margin="48,40,48,0">
            <TextBlock Text="PROJECT HUB" Style="{StaticResource Typography.Eyebrow}"/>
            <TextBlock Text="Your Projects" Style="{StaticResource Typography.ScreenTitle}"/>
        </StackPanel>

        <!-- Create New CTA -->
        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="48,24,48,40">
            <Button Content="+ Create New Project"
                    Command="{Binding CreateNewProjectCommand}"
                    Style="{StaticResource AppButton.Primary}"
                    Padding="32,14" Height="48"/>
        </StackPanel>

        <!-- Project tiles -->
        <ScrollViewer Grid.Row="2" Grid.Column="0" Margin="40,0,40,40" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding RecentProjects}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:ActionCard
                            Width="320"
                            MinHeight="156"
                            Margin="10"
                            Title="{Binding Name}"
                            Description="{Binding Description}"
                            StatusText="{Binding IndexingStatus, TargetNullValue='Needs Index'}"
                            IconData="{StaticResource Icon.Nav.Projects}"
                            ActionText="Open"
                            PrimaryCommand="{Binding DataContext.OpenProjectCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            PrimaryCommandParameter="{Binding}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Embedded project wizard -->
        <controls:SurfacePanel Grid.Row="0" Grid.RowSpan="3" Grid.Column="1"
                Margin="0,24,32,32"
                Visibility="{Binding IsProjectWizardOpen, Converter={StaticResource BoolToVisibilityConverter}}"
                Variant="Elevated">
            <ContentControl Content="{Binding ProjectWizard}"/>
        </controls:SurfacePanel>
    </Grid>
</UserControl>
