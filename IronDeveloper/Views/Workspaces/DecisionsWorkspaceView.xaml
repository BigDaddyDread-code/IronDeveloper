<UserControl x:Class="IronDev.Agent.Views.Workspaces.DecisionsWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:IronDev.Agent.ViewModels.Workspaces"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:DecisionsWorkspaceViewModel, IsDesignTimeCreatable=False}">

    <Grid Background="{StaticResource AppBackgroundBrush}" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="WORKSPACE" Style="{StaticResource IronEyebrow}"/>
            <TextBlock Text="Decisions" Style="{StaticResource IronSectionTitle}"/>
        </StackPanel>

        <!-- Decisions list -->
        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Decisions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource IronCard}" Margin="0,0,0,10" Padding="22,16">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}"
                                           FontSize="15" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextBrush}"
                                           TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Detail}"
                                           Style="{StaticResource IronMetaLabel}"
                                           TextWrapping="Wrap" Margin="0,8,0,4" Opacity="0.8"/>
                                <TextBlock Text="{Binding Rationale}"
                                           Foreground="{StaticResource FaintBrush}"
                                           FontSize="12" TextWrapping="Wrap" FontStyle="Italic"/>
                                <TextBlock Text="{Binding CapturedDate, StringFormat='Captured {0:d MMM yyyy}'}"
                                           Style="{StaticResource IronMetaLabel}"
                                           HorizontalAlignment="Right" Margin="0,10,0,0" Opacity="0.5"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
