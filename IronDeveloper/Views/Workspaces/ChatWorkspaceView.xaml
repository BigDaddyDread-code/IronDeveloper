<UserControl x:Class="IronDev.Agent.Views.Workspaces.ChatWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:IronDev.Agent.ViewModels.Workspaces"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ChatWorkspaceViewModel, IsDesignTimeCreatable=False}">

    <Grid Background="{StaticResource AppBackgroundBrush}" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="WORKSPACE" Style="{StaticResource IronEyebrow}"/>
            <TextBlock Text="Chat" Style="{StaticResource IronSectionTitle}"/>
        </StackPanel>

        <!-- Messages -->
        <Border Grid.Row="1" Style="{StaticResource IronCard}" Padding="0">
            <ScrollViewer x:Name="MessagesScroller" Padding="16">
                <ItemsControl ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,12">
                                <Border x:Name="Bubble" MaxWidth="560" Padding="14,10"
                                        CornerRadius="12" HorizontalAlignment="Left"
                                        Background="#1A222C"
                                        BorderBrush="{StaticResource DarkBorderBrush}"
                                        BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Role}" FontSize="10" FontWeight="Bold"
                                                   Foreground="{StaticResource MutedBrush}"
                                                   Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding MessageText, Mode=OneWay}"
                                                 TextWrapping="Wrap"
                                                 IsReadOnly="True"
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 AcceptsReturn="True"
                                                 VerticalScrollBarVisibility="Disabled"
                                                 HorizontalScrollBarVisibility="Disabled"
                                                 FontSize="14"
                                                 Foreground="{StaticResource TextBrush}"/>
                                        <TextBlock Text="{Binding Timestamp, StringFormat=t}"
                                                   HorizontalAlignment="Right" FontSize="10"
                                                   Foreground="{StaticResource FaintBrush}" Margin="0,6,0,0"/>

                                        <Expander Header="{Binding ContextHeader}"
                                                  Visibility="{Binding HasContext, Converter={StaticResource BoolToVisibilityConverter}}"
                                                  Foreground="{StaticResource MutedBrush}"
                                                  Margin="0,8,0,0">
                                            <StackPanel Margin="0,8,0,0">
                                                <ItemsControl ItemsSource="{Binding ContextPacket.Snippets}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="#1f2937" CornerRadius="4" Padding="8" Margin="0,0,0,8">
                                                                <TextBlock Text="{Binding}" TextWrapping="Wrap" FontSize="11" Foreground="#d1d5db" />
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                                <ItemsControl ItemsSource="{Binding ContextPacket.Tickets}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="#374151" CornerRadius="4" Padding="8" Margin="0,0,0,8">
                                                                <TextBlock Text="{Binding}" TextWrapping="Wrap" FontSize="12" Foreground="#f3f4f6" />
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                                <ItemsControl ItemsSource="{Binding ContextPacket.Decisions}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="#111827" CornerRadius="4" Padding="8" Margin="0,0,0,8">
                                                                <TextBlock Text="{Binding}" TextWrapping="Wrap" FontSize="12" Foreground="#f9fafb" />
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Expander>

                                        <!-- Chat actions (assistant only) -->
                                        <StackPanel x:Name="ChatActions" Orientation="Horizontal" Margin="0,8,0,0" Visibility="Collapsed">
                                            <Button Content="📋 Create Ticket"
                                                    Command="{Binding DataContext.CreateTicketCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"
                                                    Padding="12,4" Margin="0,0,6,0"
                                                    FontSize="11" Cursor="Hand"
                                                    Background="#1E3A5F" Foreground="#93C5FD"
                                                    BorderBrush="#2563EB" BorderThickness="1"/>
                                            <Button Content="💡 Save Decision"
                                                    Command="{Binding DataContext.SaveDecisionCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"
                                                    Padding="12,4"
                                                    FontSize="11" Cursor="Hand"
                                                    Background="#1A2E1A" Foreground="#86EFAC"
                                                    BorderBrush="#22C55E" BorderThickness="1"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Grid>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding Role}" Value="user">
                                    <Setter TargetName="Bubble" Property="HorizontalAlignment" Value="Right"/>
                                    <Setter TargetName="Bubble" Property="Background" Value="#1E3A5F"/>
                                    <Setter TargetName="Bubble" Property="BorderBrush" Value="#2563EB"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Role}" Value="assistant">
                                    <Setter TargetName="ChatActions" Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Input row -->
        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0"
                     Text="{Binding PromptText, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" MinHeight="48"
                     VerticalScrollBarVisibility="Auto" Margin="0,0,12,0"/>
            <Button Grid.Column="1"
                    Content="Send"
                    Command="{Binding SendMessageCommand}"
                    Style="{StaticResource IronPrimaryButton}"
                    Padding="32,0" MinHeight="48"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"/>
        </Grid>
    </Grid>
</UserControl>
