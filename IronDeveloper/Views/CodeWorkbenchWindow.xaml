<Window x:Class="IronDev.Agent.Views.CodeWorkbenchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:IronDev.Agent.ViewModels"
        mc:Ignorable="d"
        Title="Code Workbench Sandbox" Height="800" Width="1400"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=viewmodels:CodeWorkbenchViewModel, IsDesignTimeCreatable=False}">
    
    <Grid Margin="12" Background="{StaticResource Background/App}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="320" />
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Ticket & Plan -->
        <Border Grid.Column="0" Style="{StaticResource SurfacePanel.Subtle}" Padding="16" Margin="0,0,12,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="Target Outline" Style="{StaticResource Typography.SectionTitle}" />
                
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Loaded Ticket Info -->
                        <Border Background="{StaticResource Background/Interactive}" BorderBrush="{StaticResource Border/Subtle}"
                                BorderThickness="1" CornerRadius="{StaticResource Radius/Large}" Padding="12" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="Loaded Ticket" FontSize="11" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding LoadedTicket.Title}" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,8" />
                                <TextBlock Text="{Binding LoadedTicket.Problem}" Foreground="{StaticResource Text/Secondary}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <!-- Implementation Plan Details -->
                        <Border Background="{StaticResource Background/Interactive}" BorderBrush="{StaticResource Border/Subtle}"
                                BorderThickness="1" CornerRadius="{StaticResource Radius/Large}" Padding="12">
                            <StackPanel>
                                <TextBlock Text="Implementation Plan" FontSize="11" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,4"/>
                                
                                <TextBlock Text="Target Block:" Style="{StaticResource Typography.Eyebrow}" />
                                <TextBox Text="{Binding SelectedTargetFilePath}" MinHeight="36" IsReadOnly="True" Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Strategy Summary:" Style="{StaticResource Typography.Eyebrow}" />
                                <TextBlock Text="{Binding ImplementationPlan.Summary}" Foreground="{StaticResource Text/Secondary}" TextWrapping="Wrap" Margin="0,0,0,10"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
                
                <Button Grid.Row="2" Content="Generate Plan" Command="{Binding GeneratePlanCommand}"
                        Style="{StaticResource AppButton.Primary}" MinHeight="40" Margin="0,10,0,0" />
            </Grid>
        </Border>

        <!-- Center Panel: Draft Editors -->
        <Border Grid.Column="1" Style="{StaticResource SurfacePanel.Subtle}" Padding="16" Margin="0,0,12,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Draft Workbench" Style="{StaticResource Typography.SectionTitle}" />

                <TabControl Grid.Row="1" Margin="0,0,0,12" Background="Transparent" BorderThickness="0">
                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Padding" Value="15,8" />
                            <Setter Property="FontSize" Value="14" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="0,0,0,2" />
                            <Setter Property="BorderBrush" Value="Transparent" />
                            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Foreground" Value="#2563EB" />
                                    <Setter Property="BorderBrush" Value="#2563EB" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TabControl.Resources>
                    
                    <TabItem Header="Code Draft">
                        <Border Background="{StaticResource Background/App}" BorderBrush="{StaticResource Border/Subtle}" BorderThickness="1" CornerRadius="{StaticResource Radius/Medium}" Padding="10">
                            <TextBox Text="{Binding GeneratedCode, UpdateSourceTrigger=PropertyChanged}" 
                                     AcceptsReturn="True" AcceptsTab="True" 
                                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                                     FontFamily="Consolas" FontSize="13" Background="Transparent" BorderThickness="0" />
                        </Border>
                    </TabItem>
                    <TabItem Header="Test Draft">
                        <Border Background="{StaticResource Background/App}" BorderBrush="{StaticResource Border/Subtle}" BorderThickness="1" CornerRadius="{StaticResource Radius/Medium}" Padding="10">
                            <TextBox Text="{Binding GeneratedTestCode, UpdateSourceTrigger=PropertyChanged}" 
                                     AcceptsReturn="True" AcceptsTab="True" 
                                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                                     FontFamily="Consolas" FontSize="13" Background="Transparent" BorderThickness="0" />
                        </Border>
                    </TabItem>
                </TabControl>
                
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Draft Code" Command="{Binding GenerateCodeDraftCommand}"
                            Style="{StaticResource AppButton.Primary}" MinWidth="130" Margin="0,0,10,0" />
                    <Button Content="Draft Test" Command="{Binding GenerateTestDraftCommand}"
                            Style="{StaticResource AppButton.Secondary}" MinWidth="130" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Right Panel: Validation Log -->
        <Border Grid.Column="2" Style="{StaticResource SurfacePanel.Subtle}" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Console &amp; Validation" Style="{StaticResource Typography.SectionTitle}" />
                
                <Border Grid.Row="1" Background="{StaticResource Background/App}" BorderBrush="{StaticResource Border/Subtle}" BorderThickness="1" CornerRadius="{StaticResource Radius/Large}" Padding="12" Margin="0,0,0,10">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBlock Text="{Binding OutputLog}" FontFamily="Consolas" FontSize="12" Foreground="{StaticResource Text/Primary}" TextWrapping="Wrap" />
                    </ScrollViewer>
                </Border>
                
                <Button Grid.Row="2" Content="Clear Console" Command="{Binding ClearLogCommand}"
                        Style="{StaticResource AppButton.Secondary}" MinHeight="40" />
            </Grid>
        </Border>
        
        <!-- Loading Overlay -->
        <Border Grid.ColumnSpan="3" Background="#CC000000" Panel.ZIndex="100">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBusy}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Border Background="{StaticResource Background/Elevated}" BorderBrush="{StaticResource Accent/Primary}" BorderThickness="1" CornerRadius="{StaticResource Radius/Large}" Padding="30,20" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="AI IS THINKING..." Foreground="White" FontWeight="Bold" FontSize="16" />
            </Border>
        </Border>
    </Grid>
</Window>
