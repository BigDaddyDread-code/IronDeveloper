---
id: CSHARP_DOGFOOD_RUNNER_143
project: IronDev
title: C# Dogfood Runner 143
document_type: ArchitectureProof
authority: Accepted
status: Accepted
dogfood_run_id: CSharpDogfoodRunner143
created_utc: 2026-05-23T21:43:00Z
primary_retrieval_questions:
  - What changed in C# Dogfood Runner 143?
  - Is PowerShell still the primary Test Agent runner?
  - What command should Codex use to run dogfood plans?
  - Where do C# runner reports and evidence land?
boundary: Dogfood execution layer only. PowerShell remains compatibility wrapper; no retrieval, builder, governance, memory, or real repo write authority changes.
---

# C# Dogfood Runner 143

This slice moves the primary Test Agent/dogfood plan execution path from PowerShell into `IronDev.ReplayRunner`.

Primary commands:

- `test run-plan --plan <path> --run-id <run> --json`
- `dogfood run-plan --plan <path> --run-id <run> --json`
- `agent tester run-plan --plan <path> --run-id <run> --json`

`Invoke-TestAgentPlan.ps1` now delegates to `test run-plan`.

`Invoke-TestAgentPlan.Legacy.ps1` preserves the previous implementation for explicit compatibility fallback while older actions are ported.

Each C# run writes standard evidence under:

```text
tools/dogfood/runs/{runId}/report.json
tools/dogfood/runs/{runId}/test-agent-report.json
tools/dogfood/runs/{runId}/trace.json
tools/dogfood/runs/{runId}/report.md
tools/dogfood/runs/{runId}/evidence/
tools/dogfood/runs/{runId}/logs/
```

If an unported action is encountered, the report marks `compatibility_mode: true` and records compatibility warnings.

This slice does not change Test Agent semantics, retrieval semantics, builder behaviour, governance behaviour, agent authority, or real repository write boundaries.
